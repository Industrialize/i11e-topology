{
  "name": "i11e-topology",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -R lib",
    "test": "nodeunit test",
    "build": "babel src -d lib",
    "dev-build": "babel src -d lib --source-maps"
  },
  "author": "Bo HOU <daily.bhou@gmail.com> (https://github.com/bhou)",
  "scripts": {
    "clean": "rm -R lib",
    "test": "nodeunit test & nodeunit test/examples & nodeunit test/robots",
    "build": "babel src -d lib",
    "dev-build": "babel src -d lib --source-maps"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.4",
    "babel-loader": "^6.2.4",
    "babel-plugin-conditional-compilation": "0.0.1",
    "babel-preset-es2015": "^6.6.0",
    "nodeunit": "^0.9.1",
    "webpack": "^1.12.14"
  },
  "dependencies": {
  }
}
